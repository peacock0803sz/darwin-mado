{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/peacock0803sz/mado/schemas/config.v1.schema.json",
  "title": "Mado Configuration",
  "description": "Configuration schema for ~/.config/mado/config.yaml",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "timeout": {
      "type": "string",
      "description": "AX operation timeout (e.g. 5s, 10s, 1m)",
      "pattern": "^[0-9]+(ns|us|ms|s|m|h)$",
      "default": "5s",
      "examples": ["5s", "10s", "30s"]
    },
    "format": {
      "type": "string",
      "description": "Default output format",
      "enum": ["text", "json"],
      "default": "text"
    },
    "presets": {
      "type": "array",
      "description": "Named window layout presets",
      "items": {
        "type": "object",
        "required": ["name", "rules"],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "description": "Preset name (alphanumeric, hyphens, underscores)",
            "pattern": "^[a-zA-Z0-9][a-zA-Z0-9_-]*$"
          },
          "description": {
            "type": "string",
            "description": "Human-readable description of the preset"
          },
          "rules": {
            "type": "array",
            "description": "Window operation rules (evaluated in order, first match wins)",
            "minItems": 1,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "oneOf": [
                { "required": ["app"] },
                { "required": ["app_id"] }
              ],
              "properties": {
                "app": {
                  "type": "string",
                  "description": "Application display name (case-insensitive exact match). Mutually exclusive with app_id."
                },
                "app_id": {
                  "type": "string",
                  "description": "macOS bundle identifier (case-insensitive exact match, e.g. com.apple.Safari). Mutually exclusive with app."
                },
                "title": {
                  "type": "string",
                  "description": "Window title filter (case-insensitive partial match)"
                },
                "screen": {
                  "type": "string",
                  "description": "Screen ID or name filter"
                },
                "desktop": {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Desktop number to scope this rule to (0 = windows assigned to all desktops)"
                },
                "position": {
                  "type": "array",
                  "description": "Target position [x, y] in global coordinates",
                  "items": { "type": "integer" },
                  "minItems": 2,
                  "maxItems": 2
                },
                "size": {
                  "type": "array",
                  "description": "Target size [width, height] (positive integers)",
                  "items": { "type": "integer", "minimum": 1 },
                  "minItems": 2,
                  "maxItems": 2
                }
              },
              "anyOf": [
                { "required": ["position"] },
                { "required": ["size"] }
              ]
            }
          }
        }
      }
    },
    "ignore_apps": {
      "type": "array",
      "description": "Applications to exclude from list output and preset matching. Entries containing a dot (.) are matched against bundle identifiers (e.g. com.apple.Safari); others are matched against display names (e.g. Safari).",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": false,
      "default": []
    }
  }
}
